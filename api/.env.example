## Copy to `api/.env` and fill values. DO NOT COMMIT real secrets.
# Argon2 parameters (optional)
ARGON2_TIME_COST=
ARGON2_MEMORY_COST=
ARGON2_PARALLELISM=

# Optional global pepper to append to passwords before hashing (keep secret)
PASSWORD_PEPPER=

# Database URL (example):
# DATABASE_URL=postgresql://user:pass@localhost:5432/dbname
# Example environment variables for the API
# Copy to .env and fill values before running
DATABASE_URL=postgres://user:password@db-host:5432/dbname
USE_SQLITE=0
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_KEY=public-anon-key-or-service-role-key
SECRET_KEY=replace-with-a-secure-random-value
PORT=5000

# Argon2 and password hardening (customize as needed)
ARGON2_TIME_COST=
ARGON2_MEMORY_COST=
ARGON2_PARALLELISM=
# Optional pepper (DO NOT commit real pepper to repo)
PASSWORD_PEPPER=
